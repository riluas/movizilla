import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.movizilla.app',
  appName: 'movizilla',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
