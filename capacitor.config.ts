import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.movizilla.es',
  appName: 'Movizilla',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
